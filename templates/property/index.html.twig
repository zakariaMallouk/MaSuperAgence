{% extends 'base.html.twig' %}

{% block title 'Gérer les biens'%}

{% block body %}
    <div class="conatainer mt-4">
        <h1>Gérer les biens</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>titre</th>
                    <th>Actionxw</th>
                </tr>
            </thead>
            <tbody>
                {# {% for property in properties %}
                <tr>
                    <td>{{ property.title }}
                    <td>
                        <a href="{{ path('admin.property.edit', {id: properties.id}) }}" class="btn btn-secondary">Editer</a>
                    </td>
                </tr>
                {% endfor %} #}
            </tbody>
        </table>
    </div>
{% endblock %}